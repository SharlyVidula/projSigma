const express = require('express'); const cors = require('cors'); const mongoose = require('mongoose'); const app = express(); const PORT = process.env.PORT || 3005; app.use(cors()); app.use(express.static('public')); app.get('/api/guests', (req, res) => { res.json([{ id: 1, name: 'John Doe' }, { id: 2, name: 'Jane Doe' }]); }); app.get('/api/rooms', (req, res) => { res.json([{ id: 1, type: 'single', rate: 100 }, { id: 2, type: 'double', rate: 200 }]); }); app.listen(PORT, () => { console.log(`Server listening on port ${PORT}`); });